import { Button } from '@repo/ui/button';


const TestButton = () => {
  return (
    <>
      <Button appName={'test'} >
        Click me!
      </Button>
    </>
  );
};

export default TestButton;
